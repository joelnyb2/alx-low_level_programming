#include "main.h"

/**
 * _abs - A function that computes the absolute value of a number
 * @n: Accepts integer number
 *
 * Return: Always 0;
 */
int _abs(int n)
{


	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
