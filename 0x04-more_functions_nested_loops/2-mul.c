#include "main.h"
/**
 * mul - takes two number to multiply
 * @n1: first number as argument
 * @n2: second number as argument
 * Return: the sum of the two numbers
 */
int mul(int n1, int n2)
{
	return (n1 * n2);
}
